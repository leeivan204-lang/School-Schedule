# 特教班行事曆 (School Schedule)

這是一個用於產生學期行事曆的靜態網頁應用程式。提供教師版與家長版檢視模式，支援自訂學期開始日期、重要事項與月份備註，並可匯出為 PDF。

## 專案功能
- ✨ 自動產生學期週次對照表
- 📅 支援自訂學期開始日期
- 👥 教師版/家長版切換 (過濾教師專用事項)
- 📝 新增/刪除行事曆事項與備註
- 🖨️ 匯出 PDF 列印功能
- 💾 使用 LocalStorage 自動儲存資料

## 開發環境設定

### 前置需求
此專案需要 Python 環境。
請確保已安裝 **Python 3**。

檢查版本：
```bash
python --version
```

### 啟動方式
1. 直接執行啟動腳本 (Windows 使用 `py`, macOS/Linux 使用 `python3`)：
   ```bash
   # Windows
   py dev_server.py
   
   # macOS / Linux
   python3 dev_server.py
   ```
   伺服器啟動後會自動開啟瀏覽器。

2. 或者使用 Python 內建模組啟動：
   ```bash
   py -m http.server 8000
   ```
   然後開啟瀏覽器訪問 `http://localhost:8000`。

## 部署流程 (GitHub Pages)

### 前置設定 (必讀)
在第一次部署之前，必須先設定 GitHub Pages 的來源：
1. 進入 GitHub Repository 的 **Settings** (設定)。
2. 在左側選單點選 **Pages**。
3. 在 **Build and deployment** > **Source** 下拉選單中，選擇 **GitHub Actions**。

### 自動部署
本專案已設定 GitHub Actions 自動部署流程。

1. 將程式碼推送至 GitHub 的 `main` 分支。
2. GitHub Actions 會自動觸發 `pages-build-deployment` workflow。
3. 部署完成後，可於 Repository 的 Settings > Pages 查看線上網址。

## 檔案結構
- `index.html`: 主頁面結構
- `style.css`: 樣式表
- `script.js`: 核心邏輯 (行事曆生成、資料處理)
- `.github/workflows/deploy.yml`: 自動部署設定
